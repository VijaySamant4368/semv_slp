# Library Backend

Express, Mongoose, JWT auth. Quick start:

```bash
cp .env.example .env
npm install
npm run dev
```

Base URL: `http://localhost:4000`

Routes:
- Members: `/api/members`
- Books: `/api/books`
- Donations: `/api/donations`
- Borrows: `/api/borrows`
